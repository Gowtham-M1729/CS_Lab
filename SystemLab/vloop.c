#include<stdio.h>
int main()
{
   printf("Hello This is a valid C program");
	int i, num, j=1,k=0;
	printf ("Enter the number: ");
	scanf ("%d", &num );

	for (i=1; i<num; i++
		j=j*i;    
    while(i>0)
      k=k+i;
      
	printf("The factorial of %d is %d\n",num,j);
}
}
